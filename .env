VITE_API_BASE_URL=https://voxguardian-production.up.railway.app

// In your React component (UploadForm.jsx or similar)
const handleUpload = async (file) => {
  const formData = new FormData();
  formData.append("audio", file);  // ⚠️ Must match backend expectation

  try {
    const response = await fetch(`${import.meta.env.VITE_API_BASE_URL}/analyze`, {
      method: "POST",
      body: formData,
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const data = await response.json();
    console.log("✅ Analysis result:", data);

    // Display results in UI
    setResult(data);

  } catch (error) {
    console.error("❌ Upload failed:", error);
    setError(error.message);
  }
};